$(document).on('page:fetch', function() {
$('#page_loading').show().css( { opacity: 1.0 });
$('.container').css( { opacity: 0.50 } );
});
$(document).on('page:change', function() {
$('#page_loading').hide();
$('.container').css( { opacity: 1.0 } );
});