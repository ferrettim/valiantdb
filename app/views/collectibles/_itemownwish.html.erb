Owned by <strong><%= Itemown.where(:collectible_id => @collectible.id).pluck(:user_id).count %></strong> <% if Itemown.where(:collectible_id => @collectible.id).pluck(:user_id).count == 1 %>user<% else %>users<% end %> and in <strong><%= Itemwish.where(:collectible_id => @collectible.id).pluck(:user_id).count %></strong> <% if Itemwish.where(:collectible_id => @collectible.id).pluck(:user_id).count == 1 %>wishlist<% else %>wishlists<% end %>.