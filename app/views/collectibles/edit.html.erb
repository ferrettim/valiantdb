<div class="wellclear">
	<% if user_signed_in? && current_user.admin? %>
		Editing <%= @collectible.title %></h2>

		<%= render 'form' %>

		<%= link_to 'Show', @collectible %> 
	<% else %>
	<h1 class="text-center">You're not supposed to be here!</h1>
	<h2 class="text-center">Head back to <%= link_to "the database", root_url %>, I won't say anything.</h2>
	<% end %>
</div>