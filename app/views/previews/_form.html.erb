<%= simple_form_for(@preview) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :description %>
    <%= f.association :book %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
