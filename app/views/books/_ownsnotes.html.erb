You have <%= link_to "#", data: { toggle: "modal", target: "#mynotes" }, :title => "Add a new note" do %>
	<%= @book.comments.where(:user_id => current_user.id).count %> <%= pluralize(@book.comments.where(:user_id => current_user.id).count, "note") %> 
<% end %>
 for this book.
