<div class="wellclear">
<h3 class="text-center">4001 A.D. Reading Order (2016)</h3>
<p>Please note that the full story of this crossover unfolds in the 4001 A.D. miniseries with Rai providing additional backstory. The other tie-in titles are not necessary, but offer extra information which will help fill in some gaps in the full story of the event.</p>
<br />
<div class="container">
	<% @books.each do |e| %>
		<div class="col-md-6 col-sm-6 col-xs-12">
			<div class="col-md-3 col-sm-5 col-xs-6">
				<%= link_to e, :title => e.title + " #" + e.issue.to_s do %><%= image_tag e.image.url(:thumb) %><% end %><br /><br />
			</div>
			<div class="col-md-9 col-sm-7 col-xs-6">
				<%= link_to e, :title => e.title + " #" + e.issue.to_s do %><%= e.title %> #<%= e.issue.to_s %><% end %>
				<p>Released on <%= e.rdate.strftime("%B %d, %Y") %></p>
				<p>Written by <%= e.writer %> <% if e.writer2?%>and <%= e.writer2 %><% end %><br />
				   Art by <%= e.artist %> <% if e.artist2? %>and <%= e.artist2 %><% end %></p>
				   <strong><%= e.event %></strong> part <strong><%= e.eventpart %></strong></p>
			</div>
		</div>
	<% end %>
</div>
<div class="clearfix"></div>
</div>